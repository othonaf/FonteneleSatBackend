{
    "version": 2,
    "builds": [
        {
            "src": "tsc && node ./build/index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}